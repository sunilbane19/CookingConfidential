import { supabase as sb } from './supabase-client-legacy.js?v=1.0.0';
document.addEventListener('click',async event=>{const button=event.target?.closest?.('.cc-inbox-review');if(!button||button.dataset.image==='1'||button.dataset.docx==='1'||button.dataset.url!=='1')return;const id=Number(button.dataset.id);if(!id)return;event.preventDefault();event.stopImmediatePropagation();const dialog=document.querySelector('#detailDialog');try{if(dialog){dialog.querySelector('#detailContent').innerHTML='<div class="dialog-card"><p class="eyebrow">EXTRACTING</p><h2>Preparing recipe…</h2><p class="small-note">Reading the source post. This may take a few seconds.</p></div>';dialog.showModal();}const{error}=await sb.functions.invoke('cc-import-extract',{body:{import_item_id:id}});if(error){const q=await sb.from('cc_import_items').select('error_message').eq('id',id).single();throw Error(q.data?.error_message||error.message||'The source post could not be read.');}const mod=await import('./import-review-legacy.js?v=1.0.0');if(typeof mod.reviewImportFixed!=='function')throw Error('Review module could not be loaded.');await mod.reviewImportFixed(id);}catch(e){dialog?.close();alert(e?.message||'Could not open the source post.');}},true);
