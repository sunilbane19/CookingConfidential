import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const sb=createClient('https://yiwmtfbqbynimqvwxosu.supabase.co','sb_publishable_EG30cid4BV1Uvr6EeM3f9g_hztA7Wpu');
const norm=s=>String(s??'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
async function currentUser(){const{data:{user}}=await sb.auth.getUser();if(!user)throw Error('Please sign in again before saving.');return user}
async function existingRecipe(user,name){const{data,error}=await sb.from('cc_recipes').select('id,name').eq('created_by',user.id).ilike('name',name.trim());if(error)throw Error(error.message);return(data||[]).find(r=>norm(r.name)===norm(name))||null}
async function saveRecipe(user,name,ingredients,itemTitle){const existing=await existingRecipe(user,name);if(existing)return{saved:false,duplicate:true,id:existing.id};const{data,error}=await sb.from('cc_recipes').insert({name,description:null,ingredients,method:null,cuisine:null,course:'Ingredient',source_type:'file',source_url:null,source_title:itemTitle||'Imported image',created_by:user.id,visibility:'private'}).select('id').single();if(error){if(error.code==='23505'){const duplicate=await existingRecipe(user,name);if(duplicate)return{saved:false,duplicate:true,id:duplicate.id}}throw Error(`Could not save ${name}: ${error.message}`)}return{saved:true,duplicate:false,id:data?.id}}
async function latestImport(user){const{data}=await sb.from('cc_import_items').select('id,file_name,source_title,created_at').eq('created_by',user.id).eq('review_status','pending').eq('source_title','Four-recipe image').order('created_at',{ascending:false}).limit(1).maybeSingle();return data||null}
function values(form,i){const name=String(form.elements[`name-${i}`]?.value||'').trim();const ingredients=String(form.elements[`ingredients-${i}`]?.value||'').split(/\r?\n|\\n/).map(x=>String(x).trim()).filter(Boolean);return{name,ingredients}}
function mark(card,text){const b=card?.querySelector('.cc-save-one'),c=card?.querySelector('.cc-save-choice');if(b){b.disabled=true;b.textContent=text}if(c)c.checked=false}
async function handleOne(form,i,itemTitle){const{name,ingredients}=values(form,i);if(!name||!ingredients.length)return{saved:false,duplicate:false,invalid:true};const user=await currentUser();const result=await saveRecipe(user,name,ingredients,itemTitle);const card=form.querySelector(`[data-recipe-index="${i}"]`);mark(card,result.duplicate?'Already saved':'Saved');return result}
async function handleBulk(form){const choices=[...form.querySelectorAll('.cc-save-choice:checked')].map(x=>Number(x.dataset.index));if(!choices.length){alert('Select at least one recipe to save.');return}try{const user=await currentUser();const item=await latestImport(user);const itemTitle=item?.file_name||'Imported image';let saved=0,duplicates=0,invalid=0;for(const i of choices){const{name,ingredients}=values(form,i);if(!name||!ingredients.length){invalid++;continue}const result=await saveRecipe(user,name,ingredients,itemTitle);const card=form.querySelector(`[data-recipe-index="${i}"]`);mark(card,result.duplicate?'Already saved':'Saved');if(result.duplicate)duplicates++;else saved++}if(item)await sb.from('cc_import_items').update({recipe_id:null,review_status:'approved',extraction_status:'ready'}).eq('id',item.id);let message='Nothing was saved.';if(saved&&duplicates)message=`${saved} new recipe${saved===1?'':'s'} saved. ${duplicates} already in your library.`;else if(saved)message=`${saved} recipe${saved===1?'':'s'} saved successfully.`;else if(duplicates&&!invalid)message=`Nothing new was saved — all ${duplicates} selected recipe${duplicates===1?' is':'s are'} already in your library.`;else if(duplicates)message=`Nothing new was saved — ${duplicates} selected recipe${duplicates===1?' is':'s are'} already in your library. ${invalid} could not be saved.`;else if(invalid)message=`Nothing was saved. ${invalid} selected recipe${invalid===1?' is':'s are'} missing a name or ingredients.`;alert(message);if(item||saved)window.location.reload()}catch(e){alert(e.message||'Could not save the selected recipes.')}}
document.addEventListener('click',async event=>{const one=event.target?.closest?.('.cc-save-one');const bulk=event.target?.closest?.('#ccSaveSelected');if(!one&&!bulk)return;const form=event.target?.closest?.('#ccPosterForm');if(!form)return;event.preventDefault();event.stopImmediatePropagation();try{if(one){await handleOne(form,Number(one.dataset.index),'Imported image');return}await handleBulk(form)}catch(e){alert(e.message||'Could not save the recipe.')}},true);
