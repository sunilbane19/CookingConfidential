import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import * as mammoth from 'https://esm.sh/mammoth@1.6.0';
const SUPABASE_URL='https://yiwmtfbqbynimqvwxosu.supabase.co';
const SUPABASE_PUBLISHABLE_KEY='sb_publishable_EG30cid4BV1Uvr6EeM3f9g_hztA7Wpu';
const supabase=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY),detailDialog=document.querySelector('#detailDialog'),importDialog=document.querySelector('#importDialog');
const esc=(s='')=>String(s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const courseOptions=`<option value="">Select category…</option><optgroup label="Dishes"><option>Breakfast</option><option>Brunch</option><option>Starter</option><option>Soup</option><option>Salad</option><option>Main</option><option>Side</option><option>Snack</option><option>Dessert</option><option>Bread</option><option>Beverage</option></optgroup><optgroup label="Recipe components"><option>Dip</option><option>Dressing</option><option>Sauce</option><option>Chutney</option><option>Marinade</option><option>Rub</option><option>Paste</option><option>Spice Blend</option><option>Stock / Broth</option><option>Pickle</option><option>Condiment</option></optgroup><option value="__custom__">Other / custom…</option>`;
const cleanRefs=(value='')=>String(value).replace(/\s*\[[\s\d,;,-]+\]\s*/g,' ').replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F\uFFFD]/g,' ').replace(/[^\S\r\n]+/g,' ').replace(/\s+/g,' ').trim();
const cleanMethod=(value='')=>String(value).replace(/\s*\[[\s\d,;,-]+\]\s*/g,' ').replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F\uFFFD]/g,' ').replace(/[ \t]+\n/g,'\n').replace(/\n{3,}/g,'\n\n').trim();
function parseRecipe(x){const fileTitle=x.file_name?.replace(/\.[^.]+$/,'')||'Imported recipe';let recipe={name:fileTitle,ingredients:[],method:'',cuisine:x.inferred_cuisine||'',course:x.inferred_course||'',servings:'',description:''};if(x.extraction_status==='ready'&&x.extracted_text){try{const j=JSON.parse(x.extracted_text);const source=j.recipe&&typeof j.recipe==='object'?j.recipe:j;const extractedName=String(source.name||'').trim();const longIntro=extractedName.length>100||/\b(we will|here is|to make|using|ingredients list)\b/i.test(extractedName);recipe={...recipe,name:longIntro?fileTitle:(extractedName||fileTitle),ingredients:Array.isArray(source.ingredients||source.recipeIngredient)?(source.ingredients||source.recipeIngredient).map(v=>typeof v==='string'?cleanRefs(v):v):[],method:source.method||source.recipeInstructions||'',cuisine:source.cuisine||source.recipeCuisine||recipe.cuisine,course:source.course||source.recipeCategory||recipe.course,servings:source.servings||source.recipeYield||'',description:source.description||(longIntro?cleanRefs(extractedName):'')};if(Array.isArray(recipe.method))recipe.method=recipe.method.map(v=>typeof v==='string'?cleanMethod(v):cleanMethod(v.text||v.name||'')).join('\n');else recipe.method=cleanMethod(recipe.method);if(Array.isArray(recipe.cuisine))recipe.cuisine=recipe.cuisine.join(', ');if(Array.isArray(recipe.course))recipe.course=recipe.course.join(', ');}catch(_){}}recipe.name=cleanRefs(recipe.name);recipe.description=cleanRefs(recipe.description);return recipe;}
async function showOriginalFile(x){if(!x.file_path)return;const{data,error}=await supabase.storage.from('cooking-confidential').createSignedUrl(x.file_path,300);if(error||!data?.signedUrl)return alert(error?.message||'Could not open the original file.');const res=await fetch(data.signedUrl);if(!res.ok)return alert('Could not load the original file.');const blob=await res.blob(),name=x.file_name||'Original recipe',lower=name.toLowerCase(),back=`<button class="close" type="button" id="ccBackToReview">×</button><p class="eyebrow">ORIGINAL RECIPE</p><h2>${esc(name)}</h2><p class="small-note">Original uploaded file. It remains private and is not exposed as a public URL.</p>`;let viewer='';if(lower.endsWith('.docx')){const result=await mammoth.convertToHtml({arrayBuffer:await blob.arrayBuffer()});viewer=`<div class="original-viewer">${result.value||'<p>No readable content found.</p>'}</div>`;}else if(lower.endsWith('.pdf')||blob.type==='application/pdf'){const url=URL.createObjectURL(blob);viewer=`<iframe class="original-pdf" title="Original recipe PDF" src="${url}"></iframe>`;}else if(blob.type.startsWith('image/')){const url=URL.createObjectURL(blob);viewer=`<div class="original-viewer"><img class="original-image" alt="Original recipe" src="${url}"></div>`;}else viewer=`<pre class="original-viewer">${esc(await blob.text())}</pre>`;detailDialog.querySelector('#detailContent').innerHTML=`${back}${viewer}`;document.querySelector('#ccBackToReview').onclick=()=>showReview(x,x.id);}
function showReview(x,id){const recipe=parseRecipe(x),ing=Array.isArray(recipe.ingredients)?recipe.ingredients.map(v=>typeof v==='string'?v:[v?.amount,v?.quantity,v?.unit,v?.name].filter(Boolean).join(' ')).map(cleanRefs).filter(Boolean).join('\n'):cleanRefs(recipe.ingredients||''),selectedCourse=Array.isArray(recipe.course)?recipe.course.join(', '):String(recipe.course||'');const originalLabel=x.file_name?`View original: ${x.file_name}`:'View original',originalAction=x.file_path?`<button class="secondary" type="button" id="ccViewOriginal">${esc(originalLabel)}</button>`:'';detailDialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" onclick="detailDialog.close()">×</button><p class="eyebrow">REVIEW IMPORT</p><h2>Check the recipe before saving</h2><p class="small-note">The cleaned recipe below is the version that will be saved. The original uploaded file is retained separately.</p><form id="ccImportReviewForm"><label>Recipe name<input name="name" required value="${esc(recipe.name)}"></label><label>Description<textarea name="description" rows="4" placeholder="Optional description or introduction">${esc(recipe.description)}</textarea></label><div class="two-col"><label>Cuisine<input name="cuisine" value="${esc(recipe.cuisine)}"></label><label>Category<select name="course">${courseOptions}</select><input id="ccCustomCategory" name="custom_course" placeholder="Enter category" style="display:none;margin-top:8px"></label></div><label>Servings<input name="servings" value="${esc(recipe.servings)}"></label><label>Ingredients<textarea name="ingredients" rows="8">${esc(ing)}</textarea></label><label>Method<textarea name="method" rows="9">${esc(recipe.method)}</textarea></label><div class="detail-actions">${originalAction}<button class="secondary" type="button" id="ccCancelReview">Cancel</button><button class="primary" type="submit">Overwrite recipe</button></div></form>`;detailDialog.showModal();const form=document.querySelector('#ccImportReviewForm'),category=form.querySelector('[name="course"]'),custom=form.querySelector('[name="custom_course"]'),known=[...category.options].map(o=>o.value).filter(v=>v&&v!=='__custom__');if(selectedCourse&&known.includes(selectedCourse))category.value=selectedCourse;else if(selectedCourse){category.value='__custom__';custom.value=selectedCourse;custom.style.display='block'}category.addEventListener('change',()=>{custom.style.display=category.value==='__custom__'?'block':'none';if(category.value!=='__custom__')custom.value=''});form.addEventListener('keydown',e=>{if(e.key==='Enter'&&e.target.tagName!=='TEXTAREA'){e.preventDefault();e.stopPropagation()}});document.querySelector('#ccCancelReview').onclick=()=>detailDialog.close();const originalButton=document.querySelector('#ccViewOriginal');if(originalButton)originalButton.onclick=()=>showOriginalFile({...x,id});form.onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),{data:{user}}=await supabase.auth.getUser();if(!user)return alert('Please sign in again before saving the recipe.');const chosenCourse=String(f.get('course')==='__custom__'?f.get('custom_course'):f.get('course')).trim(),ingredients=String(f.get('ingredients')).split('\n').map(v=>cleanRefs(v.trim())).filter(Boolean),payload={name:cleanRefs(f.get('name')),description:cleanRefs(f.get('description')||'')||null,cuisine:f.get('cuisine')||null,course:chosenCourse||null,servings:f.get('servings')||null,ingredients,method:cleanMethod(f.get('method')||''),source_type:x.source_url?'social':'file',source_url:x.source_url||null,source_title:x.source_url?x.source_title:x.file_name||null,created_by:user.id,visibility:'private'};let existingId=x.recipe_id||null;if(!existingId&&x.file_name){const{data:matches}=await supabase.from('cc_recipes').select('id').eq('created_by',user.id).eq('source_type','file').eq('source_title',x.file_name).order('id',{ascending:true}).limit(1);if(matches?.length)existingId=matches[0].id}const result=existingId?await supabase.from('cc_recipes').update(payload).eq('id',existingId):await supabase.from('cc_recipes').insert(payload);if(result.error)return alert(result.error.message);const savedId=existingId||result.data?.[0]?.id;const{error:updateError}=await supabase.from('cc_import_items').update({recipe_id:savedId||null,review_status:'approved',extraction_status:'ready'}).eq('id',id);if(updateError)return alert(updateError.message);detailDialog.close();window.location.reload()};}
export async function reviewImportFixed(id){importDialog?.close();detailDialog.querySelector('#detailContent').innerHTML='<div class="dialog-card"><p class="eyebrow">REVIEW IMPORT</p><h2>Preparing recipe…</h2><p class="small-note cc-pdf-progress">Reading the uploaded recipe. This may take a few seconds.</p></div>';detailDialog.showModal();let{data:x,error}=await supabase.from('cc_import_items').select('*').eq('id',id).single();if(error){detailDialog.close();return alert(error.message)}if(!x){detailDialog.close();return alert('Could not load the uploaded recipe. Please try the import again.')}if(/\.(png|jpe?g|webp)$/i.test(x.file_name||'')||String(x.mime_type||'').startsWith('image/')){detailDialog.close();if(typeof window.ccMultiReview==='function')return window.ccMultiReview(id);try{await import('./multi-recipe-import.js?v=1.1.1');if(typeof window.ccMultiReview==='function')return window.ccMultiReview(id)}catch(e){return alert(e.message||'Could not open image review.')}return alert('Image review module is not available yet.')}const isPdf=/\.pdf$/i.test(x.file_name||'')||String(x.mime_type||'')==='application/pdf';const needsRefresh=x.extraction_status==='pending'||x.extraction_status==='processing';if(needsRefresh){const{error:fxError}=await supabase.functions.invoke('cc-import-extract',{body:{import_item_id:id}});if(fxError&&isPdf){try{const mod=await import('./scanned-pdf-ocr.js?v=1.0.0');const out=await mod.ocrScannedPdf(id);x=out.item}catch(ocrError){detailDialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" onclick="detailDialog.close()">×</button><p class="eyebrow">IMPORT ERROR</p><h2>Recipe extraction failed</h2><p class="small-note">${esc(ocrError.message||'The scanned PDF could not be read.')}</p>`;return}}else if(fxError){detailDialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" onclick="detailDialog.close()">×</button><p class="eyebrow">IMPORT ERROR</p><h2>Recipe extraction failed</h2><p class="small-note">${esc(fxError.message||'The recipe could not be extracted. Please try again.')}</p>`;return}else{const{data:refreshed,error:re}=await supabase.from('cc_import_items').select('*').eq('id',id).single();if(re)return alert(re.message);x=refreshed}}const{data:latest,error:latestError}=await supabase.from('cc_import_items').select('*').eq('id',id).single();if(latestError){detailDialog.close();return alert(latestError.message)}if(latest.extraction_status==='failed'){if(isPdf){try{const mod=await import('./scanned-pdf-ocr.js?v=1.0.0');const out=await mod.ocrScannedPdf(id);return showReview(out.item,id)}catch(e){detailDialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" onclick="detailDialog.close()">×</button><p class="eyebrow">IMPORT ERROR</p><h2>Recipe extraction failed</h2><p class="small-note">${esc(e.message||'The scanned PDF could not be read.')}</p>`;return}}detailDialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" onclick="detailDialog.close()">×</button><p class="eyebrow">IMPORT ERROR</p><h2>Recipe extraction failed</h2><p class="small-note">${esc(latest.error_message||'The recipe could not be extracted from this file.')}</p>`;return}showReview(latest,id)}
document.addEventListener('click',event=>{const button=event.target.closest('.review-btn');if(!button)return;const isImage=/\.(png|jpe?g|webp)$/i.test(button.closest('.review-item')?.querySelector('strong')?.textContent||'');if(isImage)return;event.preventDefault();event.stopImmediatePropagation();reviewImportFixed(Number(button.dataset.id))},true);
const loginForm=document.querySelector('#loginForm'),loginMessage=document.querySelector('#loginMessage');if(loginForm){loginForm.onsubmit=async e=>{e.preventDefault();const button=loginForm.querySelector('button');if(!button||button.disabled)return;const email=document.querySelector('#emailInput').value.trim();button.disabled=true;button.textContent='Sending…';if(loginMessage)loginMessage.textContent='Sending sign-in link…';const{error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:'https://cookingconfidential.in/'}});if(error){const message=String(error.message||'').toLowerCase();if(loginMessage)loginMessage.textContent=message.includes('rate limit')?'Please wait about 60 seconds before requesting another sign-in link.':'We could not send the sign-in link right now. Please try again in a moment.';button.disabled=false;button.textContent='Send me a sign-in link'}else if(loginMessage)loginMessage.textContent='Check your email for the sign-in link.'}}
