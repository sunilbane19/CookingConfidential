const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const RICH_TAGS=new Set(['B','STRONG','I','EM','U','UL','OL','LI','BR','P','DIV']);
export function sanitizeRichHtml(input=''){const raw=String(input??'');const parser=new DOMParser();const doc=parser.parseFromString(`<div>${raw}</div>`,'text/html');const cleanNode=node=>{if(node.nodeType===Node.TEXT_NODE)return document.createTextNode(node.nodeValue||'');if(node.nodeType!==Node.ELEMENT_NODE)return document.createTextNode('');if(!RICH_TAGS.has(node.tagName)){const frag=document.createDocumentFragment();[...node.childNodes].forEach(child=>frag.appendChild(cleanNode(child)));return frag}const el=document.createElement(node.tagName.toLowerCase());[...node.childNodes].forEach(child=>el.appendChild(cleanNode(child)));return el};const holder=doc.body.firstElementChild;const out=document.createElement('div');[...(holder?.childNodes||[])].forEach(node=>out.appendChild(cleanNode(node)));return out.innerHTML.trim()}
function plainToRichHtml(value=''){const text=String(value??'');if(/<\/?[a-z][\s\S]*>/i.test(text))return sanitizeRichHtml(text);return esc(text).replace(/\r\n?/g,'\n').replace(/\n/g,'<br>')}
function richFieldMarkup(f){const value=plainToRichHtml(f.value||'');const commands=[['bold','B','Bold'],['italic','I','Italic'],['underline','U','Underline'],['insertUnorderedList','•','Bulleted list'],['insertOrderedList','1.','Numbered list']];return `<label class="rich-field${f.className?` ${esc(f.className)}`:''}">${esc(f.label)}<div class="rich-toolbar" role="toolbar" aria-label="${esc(f.label)} formatting">${commands.map(([cmd,label,title])=>`<button type="button" class="rich-tool" data-command="${cmd}" title="${esc(title)}" aria-label="${esc(title)}">${label}</button>`).join('')}</div><div class="rich-editor" contenteditable="true" role="textbox" aria-multiline="true" data-rich-name="${esc(f.name)}">${value}</div><textarea class="rich-sync" name="${esc(f.name)}" hidden aria-hidden="true">${esc(value)}</textarea></label>`}
function fieldMarkup(f){const value=String(f.value??'');const required=f.required?' required':'';const cls=f.className?` class="${esc(f.className)}"`:'';if(f.type==='richtext')return richFieldMarkup(f);if(f.type==='textarea')return `<label${cls}>${esc(f.label)}<textarea name="${esc(f.name)}" rows="${f.rows||6}"${required}${f.placeholder?` placeholder="${esc(f.placeholder)}"`:''}>${esc(value)}</textarea></label>`;if(f.type==='select'){const options=Array.isArray(f.options)?f.options:[];const known=options.includes(value);const custom=f.allowCustom&&value&&!known;return `<label${cls}>${esc(f.label)}<select name="${esc(f.name)}">${options.map(o=>`<option value="${esc(o)}" ${o===value?'selected':''}>${esc(o||'Select…')}</option>`).join('')}${f.allowCustom?`<option value="__custom__" ${custom?'selected':''}>Other / custom…</option>`:''}</select>${f.allowCustom?`<input name="${esc(f.name)}_custom" placeholder="Enter category" style="display:${custom?'block':'none'};margin-top:8px" value="${custom?esc(value):''}">`:''}</label>`}return `<label${cls}>${esc(f.label)}<input type="${esc(f.type||'text')}" name="${esc(f.name)}" value="${esc(value)}"${required}${f.placeholder?` placeholder="${esc(f.placeholder)}"`:''}></label>`}
function groupMarkup(group){return `<div class="two-col">${group.map(fieldMarkup).join('')}</div>`}
export function createGenericEditor({dialog,eyebrow='EDIT',title='Edit',sourceHtml='',fields=[],actions={},onSave}){if(!dialog)throw new Error('Editor dialog not found');const content=dialog.querySelector('#detailContent');const body=[];fields.forEach(f=>body.push(f.group?groupMarkup(f.group):fieldMarkup(f)));content.innerHTML=`<button class="close" type="button" id="ccGenericEditorClose">×</button><p class="eyebrow">${esc(eyebrow)}</p><h2>${esc(title)}</h2>${sourceHtml||''}<form id="ccGenericEditorForm">${body.join('')}<div class="detail-actions"><button class="secondary" type="button" id="ccGenericEditorCancel">${esc(actions.cancelLabel||'Cancel')}</button>${actions.delete?`<button class="secondary" type="button" id="ccGenericEditorDelete">${esc(actions.deleteLabel||'Delete')}</button>`:''}<button class="primary" type="submit">${esc(actions.saveLabel||'Save changes')}</button></div></form>`;content.scrollTop=0;dialog.showModal();content.scrollTop=0;const form=content.querySelector('#ccGenericEditorForm');const close=()=>dialog.close();content.querySelector('#ccGenericEditorClose').onclick=close;content.querySelector('#ccGenericEditorCancel').onclick=close;if(actions.delete&&actions.onDelete)content.querySelector('#ccGenericEditorDelete').onclick=()=>actions.onDelete();content.querySelectorAll('select').forEach(sel=>{const custom=form.querySelector(`[name="${sel.name}_custom"]`);if(!custom)return;const sync=()=>{custom.style.display=sel.value==='__custom__'?'block':'none';if(sel.value!=='__custom__')custom.value=''};sel.addEventListener('change',sync);sync()});content.querySelectorAll('.rich-editor').forEach(editor=>{const name=editor.dataset.richName,hidden=form.querySelector(`textarea[name="${CSS.escape(name)}"]`);const sync=()=>{if(hidden)hidden.value=sanitizeRichHtml(editor.innerHTML)};editor.addEventListener('input',sync);editor.addEventListener('blur',sync);editor.querySelectorAll('a').forEach(a=>a.removeAttribute('href'));editor.parentElement.querySelectorAll('.rich-tool').forEach(button=>{button.addEventListener('mousedown',e=>e.preventDefault());button.addEventListener('click',()=>{editor.focus();document.execCommand(button.dataset.command,false,null);sync()})})});form.onsubmit=async e=>{e.preventDefault();content.querySelectorAll('.rich-editor').forEach(editor=>{const hidden=form.querySelector(`textarea[name="${CSS.escape(editor.dataset.richName)}"]`);if(hidden)hidden.value=sanitizeRichHtml(editor.innerHTML)});await onSave(new FormData(form),form)};return{form,close}}
export const editorValue=(formData,name,allowCustom=false)=>{const v=String(formData.get(name)||'').trim();return allowCustom&&v==='__custom__'?String(formData.get(`${name}_custom`)||'').trim():v};
