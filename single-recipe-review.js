import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const sb=createClient('https://esm.sh/@supabase/supabase-js@2' ? 'https://yiwmtfbqbynimqvwxosu.supabase.co' : '', 'sb_publishable_EG30cid4BV1Uvr6EeM3f9g_hztA7Wpu');
const dialog=document.querySelector('#detailDialog');
const clean=s=>String(s??'').replace(/\\[rnt]/g,m=>m==='\\n'?'\n':' ').replace(/[\u0000-\u001F\u007F\uFFFD]/g,' ').replace(/[ \t]+/g,' ').trim();
const lines=s=>String(s??'').replace(/\\n/g,'\n').replace(/\\r/g,'\n').replace(/\r/g,'').split(/\n/).map(clean).filter(Boolean);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ingredient=v=>clean(typeof v==='object'?[v.amount,v.quantity,v.unit,v.name,v.ingredient].filter(Boolean).join(' '):v).replace(/^[«»+•·▪◦\-–—*"'`:;.]+\s*/,'').trim();
const heading=/^(ingredients?|recipe ingredients?|ingredient list|method|directions?|instructions?|preparation|steps?|procedure|notes?|tips?|storage|serving suggestions?|serves?|yield)\s*[:\-–—,]?\s*$/i;
const garbage=/^(?:#\w+\s*)+$|^[^A-Za-z0-9]{0,4}$|^(?:like|comment|share|send|home|video|friends|marketplace|notifications|menu|follow|save|add a comment|see more|suggested for you|sponsored|original audio|reels?)$/i;
const timeLine=/^(?:\d{1,2}:\d{2})(?:\s*(?:am|pm))?$/i;
const dateLine=/^(?:\d{1,2}\s+(?:jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)[a-z]*\s+\d{4}|\d{1,2}\/\d{1,2}\/\d{2,4})$/i;
const socialLine=/^(?:swadish|follow|write a comment|recipe by|original audio|\d+[kKmM]?\s*(?:likes?|comments?|shares?))$/i;
function cleanIngredientBlock(value){const raw=Array.isArray(value)?value.flatMap(v=>lines(typeof v==='string'?v:ingredient(v))):lines(value);const out=[];for(const rawLine of raw){const v=ingredient(rawLine);if(!v||heading.test(v)||garbage.test(v)||/^serves?\s+\d/i.test(v)||/^recipe ingredients?$/i.test(v))continue;if(/^instructions?\s*[,.:]?$/i.test(v)||/^method\s*[,.:]?$/i.test(v)){break}if(!out.some(x=>x.toLowerCase()===v.toLowerCase()))out.push(v)}return out}
function cleanMethodBlock(value){const raw=Array.isArray(value)?value.flatMap(v=>lines(typeof v==='string'?v:ingredient(v))):lines(value);return [...new Set(raw.map(clean).filter(v=>v&&!heading.test(v)&&!garbage.test(v)&&!socialLine.test(v)))]}
function filenameTitle(fileName){return String(fileName||'Imported recipe').replace(/\.[^.]+$/,'').replace(/[_-]+/g,' ').replace(/\s+/g,' ').trim()||'Imported recipe'}
function titleIsUsable(name,fileName){const v=clean(name);if(!v||timeLine.test(v)||dateLine.test(v)||socialLine.test(v)||/^\d{1,4}$/.test(v)||heading.test(v)||v.length<3||v.length>100)return false;const letters=(v.match(/[A-Za-z]/g)||[]).length;if(letters<4)return false;const file=filenameTitle(fileName);if(file.split(/\s+/).length>=2&&!/[A-Za-z]/.test(file))return false;return true}
function bestName(r,fileName){const extracted=clean(r?.name);const file=filenameTitle(fileName);if(!titleIsUsable(extracted,fileName))return file;if(timeLine.test(extracted)||dateLine.test(extracted))return file;const recipeWord=/\b(sauce|dip|dressing|curry|chutney|marinade|rub|paste|pesto|bread|cake|salad|soup|rice|chicken|fish|meat|pasta|dessert|pakora|wada|vada|pickle|relish|stew)\b/i;if(recipeWord.test(file)&&!recipeWord.test(extracted))return file;return extracted}
export async function reviewSingleRecipe(id){
 const {data:x,error}=await sb.from('cc_import_items').select('*').eq('id',id).single();if(error||!x)return false;
 let j={};try{j=JSON.parse(x.extracted_text||'{}')}catch{return false}
 const r=j.recipe&&typeof j.recipe==='object'?j.recipe:(Array.isArray(j.recipes)&&j.recipes.length===1?j.recipes[0]:j);if(!r||Array.isArray(j.recipes)&&j.recipes.length!==1)return false;
 let ingredients=cleanIngredientBlock(r.ingredients||r.recipeIngredient||[]),method=cleanMethodBlock(r.method||r.recipeInstructions||'');
 const marker=ingredients.findIndex(v=>/^(instructions?|method|directions?)\s*[,.:]?$/i.test(v));if(marker>=0){method=[...method,...ingredients.slice(marker+1)];ingredients=ingredients.slice(0,marker)}
 const recipe={name:bestName(r,x.file_name),cuisine:clean(Array.isArray(r.cuisine)?r.cuisine.join(', '):r.cuisine),course:clean(Array.isArray(r.course)?r.course.join(', '):r.course),servings:clean(r.servings||r.recipeYield),ingredients,method,notes:cleanMethodBlock(r.notes||'')};
 dialog.querySelector('#detailContent').innerHTML=`<button class="close" type="button" aria-label="Close">×</button><p class="eyebrow">REVIEW RECIPE</p><h2>Check before saving</h2><p class="small-note">Only the cleaned fields below will be saved.</p><form id="ccSingleReview"><label>Recipe name<input name="name" required value="${esc(recipe.name)}"></label><div class="two-col"><label>Cuisine<input name="cuisine" value="${esc(recipe.cuisine)}"></label><label>Course<input name="course" value="${esc(recipe.course)}"></label></div><label>Servings<input name="servings" value="${esc(recipe.servings)}"></label><label>Ingredients<textarea name="ingredients" rows="10">${esc(recipe.ingredients.join('\n'))}</textarea></label><label>Method<textarea name="method" rows="10">${esc(recipe.method.join('\n'))}</textarea></label><label>Notes<textarea name="notes" rows="6">${esc(recipe.notes.join('\n'))}</textarea></label><div class="detail-actions cc-multi-detail-actions"><button class="secondary" type="button" id="ccSingleCancel">Cancel</button><button class="primary" type="submit">Save recipe</button></div></form>`;
 dialog.showModal();dialog.querySelector('.close').onclick=()=>dialog.close();dialog.querySelector('#ccSingleCancel').onclick=()=>dialog.close();
 dialog.querySelector('#ccSingleReview').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,fd=new FormData(f),{data:{user}}=await sb.auth.getUser();if(!user)return alert('Please sign in again.');const name=clean(fd.get('name')),ingredients=lines(fd.get('ingredients')).map(ingredient).filter(Boolean),method=lines(fd.get('method')),notes=lines(fd.get('notes'));const payload={name,cuisine:clean(fd.get('cuisine'))||null,course:clean(fd.get('course'))||null,servings:clean(fd.get('servings'))||null,ingredients,method:method.join('\n')||null,personal_notes:notes.join('\n')||null,source_type:x.source_url?'social':'file',source_url:x.source_url||null,source_title:x.source_url?(x.source_title||null):(x.file_name||null),created_by:user.id,visibility:'private'};const btn=f.querySelector('[type="submit"]');btn.disabled=true;btn.textContent='Saving…';try{const{data:existing}=await sb.from('cc_recipes').select('id').eq('created_by',user.id).eq('source_type',payload.source_type).eq('source_title',payload.source_title||'').eq('name',name).limit(1);const existingId=existing?.[0]?.id;const result=existingId?await sb.from('cc_recipes').update(payload).eq('id',existingId):await sb.from('cc_recipes').insert(payload).select('id').single();if(result.error)throw Error(result.error.message);const recipeId=existingId||result.data?.id;const upd=await sb.from('cc_import_items').update({recipe_id:recipeId||null,review_status:'approved',extraction_status:'ready',source_title:name}).eq('id',id);if(upd.error)throw Error(upd.error.message);dialog.close();window.dispatchEvent(new CustomEvent('cc:recipes-changed'));}catch(err){btn.disabled=false;btn.textContent='Save recipe';alert(err.message||'Could not save recipe.')}};return true;
}
